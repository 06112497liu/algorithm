1. 代理分为静态代理和动态代理。
    1.1 静态代理之所以为静态，是因为目标对象和代理对象都实现了相同的接口。
    1.2 动态代理
        之所以是动态，是因为代理类没有和目标对象实现相同的接口，只接受一个目标对象，并生成代理对象。
        动态代理又分为JDK动态代理和CGLIB动态代理。
        1.2.1 JDK动态代理
            是Java原生的代理，代理类实现